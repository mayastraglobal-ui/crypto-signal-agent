# =====================================================================
#  CRYPTO SIGNAL AGENT - SETTINGS
#  This is the file you edit to change how the agent behaves.
#  Lines starting with # are notes and are ignored by the program.
# =====================================================================

account:
  size_usdt: 1000            # your trading account size (used for position-size examples)
  risk_per_trade_pct: 0.5    # max you lose if the stop-loss is hit. TradeSentry v3: 0.5% while
                             # validating and in the first live month, never above 1%
  max_open_trades: 3         # (the risk engine uses risk -> max_positions below)

market:
  quote: USDT
  min_listing_days: 180      # coin must have 180+ days of price history (skips brand-new coins)
  always_include: [BTC, ETH] # always in the signal list when they pass every rule (AGENT_PROMPT.md §4)

  # ---- Coins the agent will NEVER trade. Add or remove tickers freely. ----
  exclude_meme: [DOGE, SHIB, PEPE, FLOKI, BONK, WIF, BOME, MEME, PEOPLE, TURBO, NEIRO,
                 1000SATS, DOGS, PNUT, ACT, TRUMP, BANANAS31, BROCCOLI714, 1MBABYDOGE,
                 MUBARAK, TST, CHEEMS, POPCAT, MOODENG, PENGU, FARTCOIN, SPX, GOAT, BABYDOGE,
                 LUNC, USTC, NOT, HMSTR, CATI, BANANA, SLERF, MEW, BRETT, MOG, GIGA, WLFI]
  exclude_ai:   [FET, AGIX, OCEAN, TAO, RNDR, RENDER, WLD, ARKM, AI, NFP, AIXBT, VIRTUAL,
                 COOKIE, IO, PHB, CGPT, AI16Z, GRIFFAIN, KAITO, SAHARA, ALCH, VANA, AIOZ,
                 GLM, AKT, ATH, NMR, IQ, THETA, SWARMS, ZEREBRO, HOOK, MDT, ORAI,
                 NEAR]       # NEAR: markets itself as "the blockchain for AI" (operator decision 2026-09-24)
  exclude_other: [USDC, FDUSD, TUSD, USDP, DAI, BUSD, EUR, AEUR, EURI, USD1, USDE, XUSD,
                  BFUSD, WBTC, WBETH, BNSOL, STETH, USDS, RLUSD, PYUSD, EURC]
  exclude_gold:  [PAXG, XAUT]
  # Unknown stablecoins are ALSO caught by behaviour: see universe -> stablecoin_band_pct.

universe:                    # which coins the agent watches (AGENT_PROMPT.md section 4)
  signal_coins: 7            # top 7 eligible coins by 24h volume: the ONLY coins that can give signals
  research_coins: 10         # the 7 + the next 3: those 3 are backtested only, never a signal
  hysteresis_runs: 2         # a new coin must be in the top 7 for 2 runs in a row to join,
                             # and a member outside the top 7 for 2 runs in a row leaves.
                             # A member that breaks a rule below leaves IMMEDIATELY.
  candidate_pool: 20         # how many high-volume coins get the detailed checks each run
  # --- eligibility rules: ALL must pass ---
  min_24h_volume_usdt: 50000000     # $50M+ traded in the last 24h
  min_7d_avg_volume_usdt: 50000000  # ... and $50M+ per day on average over 7 days
  max_volume_spike_x: 3      # 24h volume above 3x the 7-day average = a one-day spike -> not eligible
  max_abs_24h_move_pct: 25   # moved more than ±25% in 24h -> suspended for the rest of the UTC day
  max_spread_pct: 0.10       # gap between best buy and best sell price
  min_depth_usdt_1pct: 250000  # at least $250k of orders on EACH side within 1% of the price
  stablecoin_band_pct: 2     # price stayed within ±2% for 30 days = behaves like a stablecoin
  stablecoin_days: 30
  wash_volume_flag_ratio: 1000 # 24h volume > 1000x the 1% order-book depth = "possible wash volume".
                               # FLAG ONLY for now (operator decision): review after a week of data.


# How many candles (bars) of history to download for each timeframe.
# More bars = more backtest trades = more reliable results (but slower).
timeframes:
  "1w": 500      # ~10 years, or since the coin was listed (higher-timeframe context)
  "1d": 1000     # ~2.7 years (higher-timeframe context + later long backtests)
  "4h": 1500     # ~250 days
  "1h": 2000     # ~83 days
  "30m": 2000    # ~42 days
  "15m": 2000    # ~21 days
  "5m": 5000     # ~17 days (Layer A needs the last 15 days)

timeframe_model:             # which timeframe does what (AGENT_PROMPT.md section 5)
  # chain = from slow to fast. The last 3 are: setup -> trigger -> entry (execution);
  # the ones before them give permission (bias). veto = a timeframe that can block a trade.
  # This is a HYPOTHESIS: models A-D are compared with identical tests in later phases.
  active: B
  models:
    A: {chain: [7d, 1d, 4h, 1h, 15m, 5m]}
    B: {chain: [1d, 4h, 1h, 30m, 15m, 5m], veto: 1w}    # default
    C: {chain: [7d, 1d, 1h, 30m, 15m, 5m]}
    D: {chain: [1d, 2h, 30m, 15m, 5m]}                  # 2h is downloaded only once D is tested

features:                    # the feature engine (AGENT_PROMPT.md section 7). ATR = average candle size (14)
  swing_n: 3                 # swing high/low = highest/lowest point with 3 candles on each side
                             # (it only "exists" once those 3 candles have closed)
  rel_vol_n: 20              # relative volume = this candle / average of the previous 20
  displacement_range_atr: 1.5  # displacement candle: range >= 1.5 x ATR,
  displacement_body_pct: 0.6   # body >= 60% of the range,
  displacement_rel_vol: 1.3    # and volume >= 1.3 x normal
  rejection_wick_body_x: 2.0   # pin bar: wick >= 2 x body and >= 60% of the range,
  rejection_wick_pct: 0.6      # close in the far third
  consolidation_n: 20        # consolidation: the last 20 candles fit inside 3 x ATR
  consolidation_atr: 3.0
  breakout_n: 20             # breakout: close beyond the previous 20-candle high / low
  retest_bars: 5             # retest: back to the broken level within 5 candles and holds
  failed_bars: 3             # failed breakout: closes back inside within 3 candles
  impulse_atr: 2.5           # impulse: a move of 2.5 x ATR within 5 candles
  impulse_bars: 5
  pullback_min: 0.382        # pullback: gives back 38.2% - 61.8% of the impulse
  pullback_max: 0.618

regime:                      # market regime per timeframe (AGENT_PROMPT.md section 6) - SHOWN ONLY for now;
                             # strategies declare allowed regimes and permission becomes a gate in Phase 7
  timeframes: [1w, 1d, 4h, 1h]
  ema: [50, 200]             # EMA structure: close vs EMA50 vs EMA200 ...
  ema_by_tf: {1w: [10, 40]}  # ... weekly uses 10/40 weeks (about the same calendar span as 50/200 days;
                             #     200 weeks = 4 years of history, which many coins do not have). Operator OK 2026-09-24
  slope_bars: 10             # EMA-fast slope over 10 candles; more than 1 ATR up = bull vote, down = bear vote
  slope_atr: 1.0
  adx_strong: 25             # ADX above 25 = strong trend
  adx_weak: 20               # ADX below 20 = weak trend / ranging
  expansion_bb_pct: 0.90     # EXPANSION: Bollinger width in its top 10%, a candle range >= 1.5x the ATR
  expansion_range_atr: 1.5   #   before it, and a displacement / breakout - all in the last 3 candles
  compression_bb_pct: 0.10   # COMPRESSION: width in its bottom 10%, ATR <= 0.8x normal, ADX below 20
  compression_atr_x: 0.8
  high_vol_atr_x: 1.3        # a range with ATR >= 1.3x normal = HIGH_VOL_RANGE

smc:                         # Smart Money Concepts / ICT (AGENT_PROMPT.md section 9) - HYPOTHESES TO TEST.
                             # Detected only; nothing trades on SMC until strategy spec v3 (Phase 7).
  equal_pct: 0.1             # two swing highs (lows) within 0.1% = equal highs (lows)
  sweep_atr: 0.1             # sweep: wick through a liquidity pool by >= 0.1 ATR, candle closes back inside
  fvg_atr: 0.25              # fair value gap must be >= 0.25 ATR
  displacement_bars: 3       # CHoCH needs a displacement candle within the last 3 candles
  ob_lookback: 10            # order block = last opposite candle within 10 candles before the displacement
  ote: [0.62, 0.79]          # optimal trade entry zone = 62% - 79% retracement of the displacement leg
  amd_bars: 10               # power of 3: consolidation -> sweep -> CHoCH within 10 candles
  log_timeframes: [4h, 1h, 30m, 15m]   # live event log: signal coins on these timeframes (operator OK)

evidence:                    # candle evidence - RESEARCH EVIDENCE, NOT A SIGNAL
  stop_atr: 1.0              # hypothetical stop 1 ATR from the entry (next candle's open)
  targets_r: [1, 2, 3]       # how often +1R / +2R / +3R was reached AFTER costs, before the stop
  max_bars: 30               # give up after 30 candles
  random_per_event: 10       # random baseline: 10 random entries per pattern entry
  min_events: 30             # fewer entries than this = "too few to judge"

costs:                       # realistic trading costs - never set these to 0!
  # Exchange not chosen yet, so we use Binance's standard (VIP 0, no BNB discount) fees.
  long:                      # LONG trades = buying on the SPOT market
    taker_fee_pct: 0.10      # market order fee (Binance spot)
    maker_fee_pct: 0.10      # limit order fee (Binance spot)
    slippage_pct: 0.05       # price moves against you when a market order fills
  short:                     # SHORT trades = FUTURES ONLY (you cannot short on spot)
    taker_fee_pct: 0.05      # market order fee (Binance USDT-M futures)
    maker_fee_pct: 0.02      # limit order fee (Binance USDT-M futures)
    slippage_pct: 0.05
    funding_pct_per_8h: 0.01 # futures "funding" fee, paid every 8 hours while the trade is open.
                             # Real funding changes sign; we cautiously ALWAYS charge it to the short.

data_quality:                # safety checks on the price data, every run (AGENT_PROMPT.md section 3)
  # GOOD = signals allowed · DEGRADED = analysis only · UNSAFE = no signals
  stale_periods: 2           # newest closed candle older than 2 candle-lengths -> UNSAFE
  max_missing_pct: 1.0       # more than 1% of candles missing -> UNSAFE
  recent_bars: 300           # any missing candle among the newest 300 -> DEGRADED
  volume_median_bars: 50     # "normal volume" = median of the 50 candles before
  volume_check_bars: 3       # check the newest 3 closed candles for volume spikes
  volume_spike_warn: 10      # 10x normal volume -> note only (real breakouts do this)
  volume_spike_degraded: 50  # 50x normal volume -> probably bad data -> DEGRADED
  cross_venue_max_pct: 0.5   # Binance vs OKX price differ by more than 0.5% -> DEGRADED
  system_unsafe_coin_share: 0.30   # BTC UNSAFE, or >30% of coins UNSAFE -> ALL signals stop
                                   # (DATA_STALE / SIGNAL_DISABLED) + a [SYSTEM] email

trade_plan:                  # the TP1 / TP2 / TP3 structure used for EVERY signal
  tp_r: [1.0, 2.0, 3.0]      # TP1 = 1R, TP2 = 2R, TP3 = 3R  (R = distance entry -> stop-loss)
  tp_split: [0.4, 0.3, 0.3]  # close 40% at TP1, 30% at TP2, 30% at TP3
  move_stop_to_breakeven_after_tp1: true
  move_stop_to_tp1_after_tp2: true

validation:                  # the "-> VALIDATION" gate (AGENT_PROMPT.md section 12), per strategy version x timeframe
  in_sample_share: 0.7       # first 70% of history = "training", last 30% = "unseen test"
  min_trades: 30             # need at least 30 backtest trades (all coins together)
  min_oos_trades: 10         # at least 10 trades in the unseen test period
  min_expectancy_r: 0.10     # must make on average +0.10R per trade AFTER fees (was 0.05 before spec v3)
  min_profit_factor: 1.2     # gross wins / gross losses (was 1.15 before spec v3)
  max_drawdown_r: 10         # worst peak-to-valley loss of the backtest, in R
  retune_penalty_r: 0.02     # +0.02R on min_expectancy_r for every earlier version of the same idea (section 11)
  max_cost_to_r: 0.25        # cost viability: fees + slippage per trade <= 0.25R, i.e. stop >= 4x the round-trip cost
  min_coin_trades: 5         # a coin needs 5+ trades with this strategy to be "proven" on it
  forward_pause_after: 20    # after 20 LIVE signals ...
  forward_pause_below_r: -0.10   # ... if live average is below -0.10R, pause the strategy

research:                    # daily research run (research.py, workflow "Research") - AGENT_PROMPT.md sections 11-12
  history_bars:              # how much history Layer B / C use (fewer when a coin is younger)
    1w: 1000
    1d: 5000                 # everything since listing (Binance starts in 2017)
    4h: 30000                # everything since listing
    1h: 100000               # everything since listing
    30m: 35040               # 2 years
    15m: 35040               # 1 year
    5m: 25920                # 90 days
  offline_history_bars: 6000 # --offline tests only (synthetic data)
  layer_a_days: 15           # Layer A = the last 15 days ...
  layer_a_split_day: 10      # ... shown as days 1-10 vs days 11-15
  walk_forward_windows: 6    # the history in 6 equal time windows; the first only warms up
  walk_forward_min_positive: 3   # pass: at least 3 of the other 5 windows profitable ...
  walk_forward_min_trades: 5     # ... (a window with fewer trades = no information) and all 5 together profitable
  cost_stress_x: 1.5         # stress test: fees, slippage and funding +50% - must stay profitable
  perturb_pct: 20            # every parameter -20% / +20%, one at a time - every variant must stay profitable
  min_positive_coins: 3      # edge (profitable, >= min_coin_trades trades) on at least 3 coins
  overfit_max_share: 0.5     # HIGH OVERFITTING RISK if > 50% of the profit comes from one coin or one window
  twin_min_trades: 30        # control-twin comparison needs this many trades on both sides ...
  twin_min_validate_trades: 10   # ... and this many in the validate part
  demote_after_failed_runs: 2    # a PAPER strategy that fails the long-history bar 2 runs in a row leaves paper
  paper_retire_last_n: 20        # RETIRED if the last 20 paper signals average ...
  paper_retire_below_r: -0.10    # ... below -0.10R
  paper_max_dd_r: 8              # ... or the paper record loses more than 8R from its best point

attribution:                 # failure attribution (AGENT_PROMPT.md section 17) - engine/attribution.py
  range_adx: 20              # range_market: ADX(14) on the trade's own timeframe below 20 (choppy)
  low_rel_vol: 0.8           # low_relative_volume: signal-candle volume below 0.8x normal
  overextended_atr: 2.0      # overextended_entry: close >= 2 ATR beyond EMA20 in the trade direction ...
  overextended_rsi: 75       # ... or RSI(14) >= 75 (long) / <= 25 (short)
  late_move_atr: 3.0         # late_entry: price already moved >= 3 ATR the trade's way ...
  late_move_bars: 10         # ... in the 10 candles before the signal
  displacement_bars: 3       # no_displacement: no strong candle in the trade direction in the last 3 (breakout/momentum/SMC)
  wide_stop_atr: 2.5         # stop_too_wide: stop wider than 2.5 ATR
  bad_target_share: 0.8      # bad_target: got 80% of the way to TP1, never hit it
  false_breakout_bars: 5     # false_breakout: closed back inside within 5 candles (breakout/momentum)
  spike_atr: 3.0             # volatility_spike: a candle >= 3 ATR big during the trade
  lag_mfe_r: 0.25            # indicator_lag: a trend/momentum trade that never got past +0.25R
  main_sessions: ["London", "NY AM", "Silver Bullet"]   # wrong_session: intraday entry outside these
  min_losses: 30             # a tag is only called systematic with >= 30 losing trades ...
  frequent_share: 0.25       # ... (loser-only tags: in >= 25% of the losses; the others: more common among
                             #     losers than winners by more than 2 standard errors)
  strong_move_atr: 5.0       # missed moves: >= 5x the 1H ATR ...
  strong_move_bars: 12       # ... within 12 hours ...
  missed_lookback_hours: 24  # ... in the last 24 hours (checked once a day)
  signal_window_bars: 3      # a signal up to 3 hours before the move started counts as "identifiable"

confirm_5m:                  # 5-minute entry confirmation (AGENT_PROMPT.md section 8), for cards with confirm_5m: true
  bars: 6                    # scan up to 6 closed 5m bars after the 15m / 30m trigger, else EXPIRED (no entry)
  min_body_pct: 0.5          # confirming bar closes in the trade direction with a body >= 50% of its range
  min_rel_vol: 1.0           # ... relative volume >= 1.0
  zone_r: 0.2                # ... and closes within the planned entry +- 0.2R

risk:                        # the risk engine (AGENT_PROMPT.md section 15) - only the operator changes these
  blackout_minutes: 60       # no live entry within +-60 min of a high-impact event (list below)
  day_limit_r: -3            # closed live results today at or below -3R -> no new live entries until tomorrow (UTC)
  week_limit_r: -6           # this week (Mon-Sun UTC) at or below -6R -> no new live entries this week
  strategy_max_dd_r: 8       # a live strategy (version x timeframe) more than 8R below its best -> SUSPENDED
  max_positions: 3           # portfolio heat: at most 3 open live positions ...
  max_per_coin: 1            # ... at most 1 per coin ...
  corr_threshold: 0.7        # ... and 1 per direction in a group of coins whose 1h returns correlate >= 0.7
  corr_bars: 720             #     (over the last 720 hourly candles = 30 days)
  min_tp1_r: 2.0             # section 14 step 9: reward to TP1 at least 2R (never widen the stop for it)
  max_risk_pct: 1.0          # account.risk_per_trade_pct is capped here ...
  first_month_risk_pct: 0.5  # ... and at 0.5% until 30 days after the first live entry
  max_leverage: 3            # never more than 3x: the position is made smaller instead
  calendar_horizon_days: 7   # the report warns when no event is listed for the next 7 days
  resume: {}                 # resume a SUSPENDED strategy: "id@version|tf": "YYYY-MM-DD" (its drawdown then
                             # counts only trades closed from that date). Example: {"S6-OB-FVG@1.0|15m": "2026-10-01"}

# High-impact events (section 14 step 11): no live entry from 60 min before to 60 min after.
# Times in UTC. Keep the next weeks filled in (official sources: bls.gov schedule for CPI / NFP,
# federalreserve.gov for FOMC). Exchange incidents: use from / until. Examples:
#   - {utc: "2026-10-14 12:30", type: CPI, name: "US CPI"}
#   - {from: "2026-10-02 08:00", until: "2026-10-02 11:00", type: exchange_incident, name: "Binance withdrawals paused"}
events: []

signals:
  lookback_bars: 2           # a signal stays valid for this many closed candles
  email_watching: false      # true = also email SETUP_FORMING / AWAITING_5M setups of APPROVED strategies ([WATCH])
  scan_interval_minutes: 60  # the scan runs hourly: every candle closed since the previous run is looked at
                             # (15m: 4 candles, 5m: 12), so no trigger falls between two runs
  max_in_report: 10          # show the best N signals
  allow_shorts: true         # false = long only (spot traders cannot short)
